{% extends 'base.html.twig' %}

{% block title %}BodyType{% endblock %}

{% block body %}
    <h1>BodyType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ body_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ body_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_body_type_index') }}">back to list</a>

    <a href="{{ path('app_body_type_edit', {'id': body_type.id}) }}">edit</a>

    {{ include('body_type/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_enviromental_badge_delete', {'id': enviromental_badge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enviromental_badge.id) }}">
    <button class="btn">Delete</button>
</form>

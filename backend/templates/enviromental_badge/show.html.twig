{% extends 'base.html.twig' %}

{% block title %}EnviromentalBadge{% endblock %}

{% block body %}
    <h1>EnviromentalBadge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enviromental_badge.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ enviromental_badge.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ enviromental_badge.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enviromental_badge_index') }}">back to list</a>

    <a href="{{ path('app_enviromental_badge_edit', {'id': enviromental_badge.id}) }}">edit</a>

    {{ include('enviromental_badge/_delete_form.html.twig') }}
{% endblock %}

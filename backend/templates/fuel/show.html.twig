{% extends 'base.html.twig' %}

{% block title %}Fuel{% endblock %}

{% block body %}
    <h1>Fuel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fuel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fuel.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fuel_index') }}">back to list</a>

    <a href="{{ path('app_fuel_edit', {'id': fuel.id}) }}">edit</a>

    {{ include('fuel/_delete_form.html.twig') }}
{% endblock %}

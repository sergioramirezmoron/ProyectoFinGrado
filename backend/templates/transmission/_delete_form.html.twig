<form method="post" action="{{ path('app_transmission_delete', {'id': transmission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transmission.id) }}">
    <button class="btn">Delete</button>
</form>
